<Window x:Class="THI_BANG_LAI_XE.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:THI_BANG_LAI_XE.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Background="White" Orientation="Vertical">
            <Button Content="🔸Home" HorizontalContentAlignment="Left" Margin="0,30,0,0" BorderThickness="0" Background="White" Padding="5"/>
            <Button Content="🔸Home" HorizontalContentAlignment="Left" Margin="2" BorderThickness="0" Background="White" Padding="5"/>
            <Button Content="🔸Home" HorizontalContentAlignment="Left" Margin="2" BorderThickness="0" Background="White" Padding="5"/>
            <Button Content="🔸Home" HorizontalContentAlignment="Left" Margin="2" BorderThickness="0" Background="White" Padding="5"/>
            <Button Content="🔸Home" HorizontalContentAlignment="Left" Margin="2" BorderThickness="0" Background="White" Padding="5"/>
            <Button Content="🔸Home" HorizontalContentAlignment="Left" Margin="2" BorderThickness="0" Background="White" Padding="5"/>
            <Button Content="🔸Home" HorizontalContentAlignment="Left" Margin="2" BorderThickness="0" Background="White" Padding="5"/>
            <Button Content="🔸Settings" HorizontalContentAlignment="Left" Margin="2" BorderThickness="0" Background="White" Padding="5"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Border Margin="0,30,0,0" CornerRadius="10, 0,0,0">
                <Border.Background>
                    <ImageBrush ImageSource="/Images/background1.png"/>
                </Border.Background>
                <ScrollViewer>
                    <DataGrid Name="DataCourse" AutoGenerateColumns="False" IsReadOnly="True" Margin="2" SelectionChanged="DataCourse_SelectionChanged">
                        <DataGrid.Background>
                            <ImageBrush></ImageBrush>
                        </DataGrid.Background>
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="*" Header="CourseName" Binding="{Binding CourseId}" Visibility="Hidden"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="CourseName" Binding="{Binding CourseName}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Lecture" Binding="{Binding Teacher.FullName}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Start" Binding="{Binding StartDate}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="End" Binding="{Binding EndDate}"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
                
            </Border>
        </Grid>
         <Frame x:Name="ContentFrame" 
               NavigationUIVisibility="Hidden" 
               Grid.Column="1"/>
    </Grid>
</Window>